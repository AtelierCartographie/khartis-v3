/**
 * Migration registry barrel.
 *
 * Import migration functions here and re-export for schema-migration.ts.
 * Migrations are applied in order during project load.
 *
 * Convention: one file per major version bump, named v{from}_to_v{to}.ts
 */
